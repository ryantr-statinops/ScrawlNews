/**
 * Named spring presets exposed through the public `motion.spring` option.
 *
 * Each entry is a `[stiffness, damping]` pair tuned against an
 * SVG-user-unit coordinate space. `crisp` is the default: snappy and close
 * to critically damped (no visible overshoot), which reads as "precise" the
 * way Linear and Figma transitions do. `gentle` is softer for large reflows;
 * `snappy` is faster with a hint of settle for small, frequent changes.
 */
export type SpringPreset = 'crisp' | 'gentle' | 'snappy';
export declare const SPRING_PRESETS: Record<SpringPreset, readonly [number, number]>;
/** Camera (viewBox) uses a softer spring than nodes so reframing never feels jerky. */
export declare const CAMERA_SPRING: readonly [number, number];
/** Per-ring stagger (ms) applied outward from a collapse/expand pivot — the "wave". */
export declare const WAVE_STAGGER_MS = 18;
/** Resolve a preset name (or an unknown value) to a `[stiffness, damping]` pair. */
export declare function resolveSpring(preset: SpringPreset | undefined): readonly [number, number];
//# sourceMappingURL=presets.d.ts.map